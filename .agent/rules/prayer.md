---
trigger: always_on
---

* Use vue.js
* Use tailwindcss for styling, separate concern and reuse duplicate styling with @apply
* Code each component in separate file / Reuse them when possible
* Each component is 1. Content 2. Styling 3. Script 
* Separate each page, related pages and its components in separate folder
* Use vue-router for navigation, router in a separated file
* Use pinia for state management, in a separated file
* Use vue-i18n and a separate file for all text content