rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /payments/{paymentId} {
      allow create: if request.resource.data.keys().hasAll(['email', 'status', 'createdAt', 'gateway']);
      allow read: if true;
      allow update, delete: if false;
    }
  }
}